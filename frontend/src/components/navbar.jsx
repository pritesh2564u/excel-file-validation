const Navbar = () => {
  return (
    <nav className="fixed top-0 left-0 w-full h-[80px] bg-white shadow-md px-4 flex items-center">
      <div>
        <h2 className="text-2xl font-bold">Excel file validator</h2>
      </div>
    </nav>
  );
};

export default Navbar;
